name: Benchmark Tracking

on:
  push:
    branches: [main]
    paths:
      - 'crates/**/*.rs'
      - 'benches/**/*.rs'
  pull_request:
    paths:
      - 'crates/**/*.rs'
      - 'benches/**/*.rs'

jobs:
  benchmark:
    name: Run Benchmarks
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y protobuf-compiler libclang-dev

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: benchmark

      - name: Run Benchmarks
        run: |
          mkdir -p bench-results
          cargo bench --workspace -- --verbose --output-format json > bench-results/benchmark-results.json

      - name: Parse Results
        id: parse
        run: |
          python3 << 'PY_EOF'
          import json, sys, os
          with open('bench-results/benchmark-results.json', 'r') as f:
              data = json.load(f)

          # Extrair métricas principais
          metrics = {}
          for bench in data.get('benchmarks', []):
              name = bench.get('name', 'unknown')
              if 'time' in bench:
                  metrics[name] = bench['time']['nanos']

          # Salvar para artefato
          with open('bench-results/metrics.json', 'w') as f:
              json.dump(metrics, f, indent=2)

          # Gerar badge
          print('::set-output name=metrics::' + json.dumps(metrics))
          PY_EOF

      - name: Upload Benchmark Results
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-results
          path: |
            bench-results/benchmark-results.json
            bench-results/metrics.json
          retention-days: 90

      - name: Create Benchmark Report
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const metrics = JSON.parse(fs.readFileSync('bench-results/metrics.json', 'utf8'));

            let report = '## 📊 Benchmark Results\n\n';
            report += '| Benchmark | Time (ns) |\n';
            report += '|-----------|-----------|\n';

            for (const [name, time] of Object.entries(metrics)) {
              report += `| ${name} | ${time.toLocaleString()} |\n`;
            }

            fs.writeFileSync('bench-results/report.md', report);

      - name: Post Report
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const report = fs.readFileSync('bench-results/report.md', 'utf8');
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: report
            });

      - name: Upload to Benchmarks GitHub Page
        if: github.ref == 'refs/heads/main'
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./bench-results
          publish_branch: gh-pages
          destination_dir: benchmarks
          keep_files: true
          force_orphan: false
